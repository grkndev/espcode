# --- Faz 4: redis-jobs / ide-builder ---
REDIS_JOBS_PASSWORD=              # openssl rand -base64 32

# --- Faz 5: ide-api (henüz kullanılmıyor) ---
# R2_ENDPOINT=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_PUBLIC_BASE=

# --- Faz 6: Postgres + GitHub OAuth (henüz kullanılmıyor) ---
# PG_PASSWORD=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# SESSION_SECRET=                 # openssl rand -base64 48
# GITHUB_APP_ID=                  # GITHUB_APP_PRIVATE_KEY .env'e değil secrets/github_app.pem'e
